seed: 42

data:
  train_dir: data/raw/train
  val_dir: null
  image_size: 224
  val_split: 0.2
  num_workers: 0

training:
  batch_size: 16
  epochs: 5
  learning_rate: 0.001
  weight_decay: 0.0001

model:
  num_classes: null
  freeze_backbone: false

output:
  dir: outputs
  checkpoint_name: best_model.pt
